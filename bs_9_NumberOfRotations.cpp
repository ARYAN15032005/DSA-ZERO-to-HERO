// Count occurence
#include<iostream>
#include<bits/stdc++.h>
using namespace std;

// brute force, check in which position there exists smallest no.
// better : find the point of breakout where there exists arr[i]<arr[i+1] by using a for loop
//optimal :

  // Count occurence
#include<iostream>
#include<bits/stdc++.h>
using namespace std;

// brute force, check in which position there exists smallest no.
// better : find the point of breakout where there exists arr[i]<arr[i+1] by using a for loop
//optimal :


  

int main(){
    int n;
    cout<<"Enter n :";
    cin>>n;

    int arr[n];
    for(int i=0;i<n;i++){
        cout<<"Enter the element "<<i+1<<":";
        cin>>arr[i];
    }
    
    

    // function name
    cout<<"\n"<<"The array is :";
    for(int i=0;i<n;i++){
        cout<<arr[i]<<" ";        
    }
    
    // RotationNo(arr,n);
    
    return 0;
    
}


int main(){
    int n;
    cout<<"Enter n :";
    cin>>n;

    int arr[n];
    for(int i=0;i<n;i++){
        cout<<"Enter the element "<<i+1<<":";
        cin>>arr[i];
    }
    
    

    // function name
    cout<<"\n"<<"The array is :";
    for(int i=0;i<n;i++){
        cout<<arr[i]<<" ";        
    }
    
    // RotationNo(arr,n);
    
    return 0;
    
}
